# Fundación (fragmento) — rutas ancladas que existen

La máquina de estados vive en `core/scripts/estado.mjs` y el runner agregado en
`tools/check.mjs`. El hook que deniega editar código sin spec es
`adapters/claude-code/hooks/require-spec.mjs`.
