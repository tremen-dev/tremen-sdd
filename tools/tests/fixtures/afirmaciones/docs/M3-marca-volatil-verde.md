# Fundación (fragmento) — la misma afirmación, en forma R

El enforcement duro es independiente del harness y vive en git pre-commit + CI
(RN-03). Su estado de implementación lo declara SPEC-002 en su frontmatter; aquí
no se narra.
