# Fundación (fragmento) — afirmación de progreso en un documento de verdad

El enforcement duro **aún no** está implementado: el destino es git pre-commit +
CI, y por ahora vive solo en los hooks del harness.
